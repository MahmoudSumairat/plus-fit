import React from "react";
import styles from "./register.module.scss";

const { registerPage } = styles;

const Register = () => {
  return <div className={registerPage}> this is the Register page </div>;
};

export default Register;
