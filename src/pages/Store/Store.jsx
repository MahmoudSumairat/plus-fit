import React from "react";
import styles from "./store.module.scss";

const { storePage } = styles;

const Store = () => {
  return <div className={storePage}> this is the Store page </div>;
};

export default Store;
