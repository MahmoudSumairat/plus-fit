import React from "react";
import styles from "./hijabs.module.scss";

const { HijabsPage } = styles;

const Hijabs = () => {
  return <div className={HijabsPage}> this is the Hijabs page </div>;
};

export default Hijabs;
