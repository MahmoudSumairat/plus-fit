import React from "react";
import styles from "./login.module.scss";

const { loginPage } = styles;

const Login = () => {
  return <div className={loginPage}> this is the Login page </div>;
};

export default Login;
